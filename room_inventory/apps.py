from django.apps import AppConfig


class RoomInventoryConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "room_inventory"
    verbose_name = "Room Inventory (Stub)"
